# Danubria TMS (Full Repo)

This is the full repository skeleton for Danubria TMS. Files are split into `backend/`, `frontend/`, `infra/`, `deploy/`, and `docs/`.

To package into a ZIP after copying files:

```bash
# from the parent of danubria-tms folder:
zip -r danubria-tms.zip danubria-tms
